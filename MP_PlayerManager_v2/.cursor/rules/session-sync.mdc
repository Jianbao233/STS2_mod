---
description: 对话结束时自动同步项目记忆与提示词记录
alwaysApply: true
---

# 会话收尾：同步记忆与提示词

每次对话结束时，判断本轮有实质性代码或文档改动后，按以下顺序执行：

## 记录层级（按优先级）

| 层级 | 文件 | 用途 |
|------|------|------|
| **1** | `K:\杀戮尖塔mod制作\STS2_mod\MP_PlayerManager\MEMORY.md` | 会话摘要，必填 |
| **2** | `K:\杀戮尖塔mod制作\STS2_mod\MP_PlayerManager\WORKFLOW_RULES.md` | 工作流/架构变更，必填 |
| **3** | `K:\杀戮尖塔mod制作\STS2_mod\MP_PlayerManager_v2\DEVLOG.md` | 细化改动记录（仅 v2 工具代码变动时填） |

## 第一步：更新 MEMORY.md

在 `## 10. 会话记录` 表格末尾追加一行：

```
| {日期} | {简短描述} | {1句话摘要} |
```

若本轮涉及重要决策，同步更新对应章节（如 `## 4. 架构决策`、`## 7. 开发备忘`）。

## 第二步：更新 WORKFLOW_RULES.md

若本轮有**架构、流程、规范层面的变更**（非纯代码修复），在文件末尾追加变更记录：

```markdown
---

## 变更记录（YYYY-MM-DD）

### 变更概要
[一句话描述]

### 详细说明
[具体变更内容]
```

## 第三步：更新 DEVLOG.md（仅 v2 Python 工具时）

在 `## {日期}` 章节下追加本轮改动：

```
### {N}. {改动主题}

**涉及文件**：`{file_path}`

**简要说明**：{1-2 句话}
```

若无当日章节则新建。

## 触发时机

- AI 回复末尾出现 `"有什么其他问题可以继续提。"` 或类似收尾语时 → **立即执行**
- 用户主动结束对话 → **立即执行**
- 每次 `StrReplace` / `Write` / `Delete` 之后 → 暂存摘要，最后一条回复时统一写入

## 注意事项

- 只记录实质变更，探索性尝试后回退的不必记录
- 若用户明确要求「不要更新记忆」→ 跳过
- 路径优先使用绝对路径避免歧义
