namespace MultiplayerTools
{
    /// <summary>
    /// 与 mod_manifest.json 的 version 字段保持同步，副标题会显示，便于确认游戏已加载新编译的 DLL。
    /// </summary>
    internal static class ModInfo
    {
        internal const string Version = "0.1.4";
    }
}
